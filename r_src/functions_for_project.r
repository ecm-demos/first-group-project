#
# Here is a shell of the functions we need for the upcoming project
#

add_two <- function (x,y){
  # adds two numbers
  # returns the sum 
  # Tom, can you work on this?
}

mult_two <- function(x, y) {
  # multiplies two numbers
  # returns the product
  # Zu, can you work on this?
}

plot_func <- function(df){
  # This should produce density plots for the continuous
  # variables in the dataframe, and histograms for the factor variables
  # and text variables
  # Who wants to handle this one?
}
